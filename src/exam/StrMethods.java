package exam;

public class StrMethods {

	public static void main(String[] args) {
		// indexOf()
		// contains(A)
		
		// equals : 두 개의 문자열이 동일한지를 비교해 결과값을 리턴
		
		// charAt(), substring(index), substring(from, to)
		
		// char replace() : 정규표현식 사용 불가능
		// String replaceAll() : 정규표현식 가능
	}

}
